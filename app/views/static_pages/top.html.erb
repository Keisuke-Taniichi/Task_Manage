<% provide(:title, "Top") %>
<div class="top-page">
  
  <h1 id="aaa" >TaskApp</h1>
   
  <center>  
  <p>TaskAppは簡易ToDo管理ツールです。</p>
  <p>↓↓初めてのご利用↓↓</p>

  <% if logged_in? %>
    <%= link_to "アカウント作成", signup_path, class: "btn btn-lg btn-primary", id: "account-cr", disabled: :true %>
  <% else %>
    <%= link_to "アカウント作成", signup_path, class: "btn btn-lg btn-primary", id: "account-cr" %>
  <% end %>
    
  </center>
</div>

<h1>ログイン</h1>
<center>
<div class="top-page">
  <div class="col-md-6 col-md-offset-3">
     
    <%= form_with(scope: :session, url: login_path, local: true) do |f| %>

      <%= f.label :email, class: "label-login" %>
      <%= f.email_field :email, class: 'form-control' %>

      <%= f.label :password, class: "label-login" %>
      <%= f.password_field :password, class: 'form-control' %>
      
      <%= f.label :remember_me, class: "checkbox inline checkbox-login", for: "remember-me" do %>
        <%= f.check_box :remember_me, id: "remember-me" %>
        <span>ログイン情報を記憶する</span>
      <% end %>
      
      <% if logged_in? %>
        <%= f.submit "ログイン状態です", class: "btn btn-primary btn-block btn-login", id: "login-btn", disabled: :true %>
      <% else %>
        <%= f.submit "ログイン", class: "btn btn-primary btn-block btn-login", id: "login-btn" %>
      <% end %>
    <% end %>

  
  </div>
</div>
  </center>